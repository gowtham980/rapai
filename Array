inlist=[]
n=int(input("Enter size of the list:\n"))
 
for i in range(0,n):
    print("Enter the number:")
    temp=int(input())
    inlist.append(temp)
print(inlist)
print(len(inlist))
for f in sorted(set(inlist)):
    if inlist.count(f)>1:
        print(f,"-",inlist.count(f))
